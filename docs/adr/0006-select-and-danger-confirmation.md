# 0006 选择器与危险确认

`select` 子命令在校验通过的候选里挑一条：单候选直接跳过选择器进入危险确认；多条弹选择器——默认内嵌 skim（`Skim::run_items` 以候选为条目跑 TUI），配置 `picker` 可切外部 fzf（候选以 NUL 分隔喂 `--read0 --print0`，因为候选可能是多行指令，换行会撑破 fzf 的行模型）。选中后若命中危险清单，先在 stdout 前弹 `⚠ 危险命令，确认? [y/N]`，只显式回答 y/Y 才输出，N、EOF、其它输入一律无输出；fzf 非零退出或空选同样无输出，都不算错误（退出码 0）。
